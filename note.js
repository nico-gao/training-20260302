/**
 * DNS lookup
 * http
 *  request
 *  response
 *
 * CRP - critical rendering path
 *
 * cookie vs session storage vs local storage
 *
 * GET: get data from server
 * POST: send data to server
 * PUT/PATCH: update the entire data object/ update partial data
 * DELETE
 *
 * status code
 *
 * 100 - informational response
 * 200 - success
 * 300 - redirection
 * 400 - client error
 *  400 - bad request
 *  401 - unauthorized
 *  403 - forbidden
 *  404 - not found
 * 500 - server error
 *
 * GET is idempotent
 *
 *
 * DOM
 * describe the structure of the document
 * manipulation, traversal, handle events
 *
 * ECMAScript - syntax standard
 * ES6
 *
 * Javascript - implementation of the standard
 *
 * JS runtime
 * browser - ECMAscript + WebAPI (DOM api, network, storage, window)
 * nodejs - ECMAScript + NodeAPI (file system, path, network(creating server), process, global)
 *
 * semantic html
 * accessibililty
 * SEO
 * meaningful code, readability, maintainability
 * aria attribute
 *
 *
 * events
 * capturing -> target element -> bubbling
 *
 * event delegation
 *
 * css specificity
 *
 * cdn (content deliver network)
 *
 * responsiveness
 * mobile first approach
 * desktop first approach
 *
 * %, vw, vh, em, rem, fr
 *
 * BEM
 *
 * card
 * card__title
 * card__description
 * card__button--disable
 * card__button--primary
 * card__button--secondary
 *
 */

/**
 * semantic versioning
 *
 * major.minor.patch
 * 5.1.12
 *
 *
 * major: breaking changes
 * minor: new features added
 * patch: bug fixing
 *
 *
 * ^: update to the latest minor version
 * ~: update to the latest patch version
 * no symbol: use the exact version
 *
 *
 *
 * REST apis
 *
 * projects -> lists -> todos
 *
 * 1. entity
 * 2. relationship, hierarchy
 * 3. crud
 *    GET, POST, PUT/PATCH/ DELETE
 *
 *
 *
 * GET /lists: get all lists
 * POST /lists: create a new list
 * PUT /lists/:listId: update a list
 * DELETE /lists/:listId: delete a list
 *
 * GET /todos: get all todos
 * GET /lists/:listId/todos: get all todos for a list
 *
 *
 * authentication: who you are
 * authorization: what you do
 *
 *
 * passing data in a request
 * 1. query parameter (filter, sort, pagination)
 * 2. path
 * 3. request body
 *
 */
